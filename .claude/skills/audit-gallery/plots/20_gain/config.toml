plot_type = "Cumulative gain curve"
dataset = "make_classification(n_samples=500, n_features=10, random_state=0), LogisticRegression(max_iter=500)"
width = 600
height = 500
panels = ["ferrum", "skp"]
ferrum_status = "READY"
ferrum_notes = "ferrum.gain_chart available; binary-only; reference diagonal (random model) expected."
