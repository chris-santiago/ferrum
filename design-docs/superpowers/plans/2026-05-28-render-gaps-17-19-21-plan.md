# Render gaps 17/18/19/21 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use chris-code:subagent-driven-development (recommended) or chris-code:executing-plans to implement this plan task-by-task.

## 1. Objective

Close code-archaeology gaps 17 (mark_hex stroke), 21 (Label scene-kind), 18 (ferrum.Grid), and 19 (Raw-in-WASM) on `feat/render-gaps-17-19-21`.

## 2. Spec references

- `design-docs/superpowers/specs/2026-05-28-render-gaps-17-19-21-design.md` — items 17/19/21, all decisions locked (§4 behavior, §6 contracts, §7 invariants, §8 decisions)
- `ferrum-spec.md §3.19` — `ferrum.Grid` constructor (~L1607-1611) + example (~L1001); item 18 has NO design spec, this is its contract
- `design-docs/superpowers/followups/2026-05-15-code-archaeology.md` — items 17/18/19/21 status + action list to update on completion

## 3. Files

| Action | Path | Reason |
|--------|------|--------|
| Modify | `src/ferrum/marks/heavy_stat.py` | item 17: desugar_hex stroke passthrough |
| Modify | `crates/ferrum-scene/src/types.rs` | items 21+19: `MarkBatchKind::Label`; `Raw{svg,anchor}` + `RawAnchor` |
| Modify | `crates/ferrum-core/src/render/marks/label.rs` | item 21: emit Label at 5 sites |
| Modify | `crates/ferrum-core/src/render/svg_walk.rs` | item 21: clip conditionals `matches!(kind, Text|Label)` |
| Modify | `crates/ferrum-wasm/src/hit_test.rs` | item 21: arm `Text|Label => hit_test_texts` |
| Create | `src/ferrum/grid.py` (or per existing pattern) | item 18: Grid value class per §3.19 |
| Modify | `src/ferrum/__init__.py` | item 18: export `Grid` in `__all__` |
| Modify | `crates/ferrum-core/src/render/marks/legend.rs`, `inset.rs`, `annotation.rs` | item 19: set Raw `anchor` |
| Modify | `crates/ferrum-wasm/src/scene_load.rs` | item 19: collect Raw into export channel |
| Modify | `src/ferrum/_wasm/ferrum-anywidget.js` | item 19: inject Raw into overlay, ID-namespace, anchor grouping |
| Test | `tests/marks/` (hex), `crates/*` (Rust units), `tests/` (Grid, interactive integration) | per-item coverage |

## 4. Constraints

- Coding agents only: `.py` → python-coder, `.rs` → rust-coder; never general-purpose. Item 19 splits Rust/JS (rust-coder) vs Python integration test (python-coder) by file boundary.
- Static SVG output for existing charts must be byte-unchanged (item 21 Label routes as Text; item 19 `svg_walk` ignores `anchor`).
- Serde back-compat: `RawAnchor` `#[serde(default)]` → `Chrome`; `Label`/`RawAnchor` cross the core↔WASM JSON boundary safely (version-pinned).
- Item 17 literal semantics: stroke color + width 0 = no visible border; NO call-time width auto-bump.
- Item 19: no new dependency / no SVG parser / no tessellator; overlay stays `pointer-events:none`.
- After code changes invoke `/regression-test` before declaring an item done.
- If any `tests/goldens/**` change: rasterize via `scripts/snapshot-goldens.py` and visually inspect the PNG before committing (CLAUDE.md).
- WASM changes require rebuild: `source ~/.cargo/env && wasm-pack build crates/ferrum-wasm --target web --out-dir ../../src/ferrum/_wasm/`.

## 5. Tasks

Build order is locked: 17 → 21 → 18 → 19.

### Task 1: Item 17 — mark_hex stroke (python-coder)
- [ ] In `desugar_hex`: remove the two `ValueError` raises; add `stroke` (if not None) and `stroke_width` (if non-zero) to the polygon layer `mark_kwargs` (spec §4, §8).
- [ ] Rewrite the two "Reserved for future use" docstring lines to document literal semantics.
- [ ] Tests: `mark_hex(stroke="#fff", stroke_width=1)` emits stroke/stroke-width attrs on hex polygons; `stroke=` alone → no visible border; old ValueError gone.
- [ ] Verify: `uv run pytest -n auto -k hex`
- [ ] `/regression-test`

### Task 2: Item 21 — MarkBatchKind::Label (rust-coder)
- [ ] Add `Label` variant to `MarkBatchKind` (serde "label").
- [ ] `label.rs`: emit `Label` at all 5 sites (4 empty-result early exits + main result).
- [ ] `svg_walk.rs`: both clip conditionals → `matches!(kind, MarkBatchKind::Text | MarkBatchKind::Label)`.
- [ ] `hit_test.rs`: merge arm → `MarkBatchKind::Text | MarkBatchKind::Label => hit_test_texts`.
- [ ] Tests: label batch kind = Label; plain text still Text; label hit-test routes via `hit_test_texts`; serde round-trip of `Label`.
- [ ] Verify: `unset CONDA_PREFIX && uv run --no-sync maturin develop` then `DYLD_LIBRARY_PATH=$(uv run python -c "import sys; print(sys.base_prefix + '/lib')") cargo test` + `uv run pytest -n auto -k "label or text"`
- [ ] `/regression-test`

### Task 3: Item 18 — gridline state exploration (Explore)
- [ ] Read `ferrum-spec.md §3.19` Grid contract.
- [ ] Map current state: relationship of existing `GridConfig` (`src/ferrum/configure.py`) to the §3.19 `Grid` value class; whether Rust layout/render actually renders gridlines today and where styling is consumed.
- [ ] Output: confirmed file targets + whether Rust wiring is needed. Gate before Task 4.

### Task 4: Item 18 — ferrum.Grid (python-coder; rust-coder iff Task 3 finds gridline rendering unwired)
- [ ] Implement `Grid` value class per §3.19 (major/minor toggles + per-level color/dash/width/opacity).
- [ ] Wire styling into the gridline-consumption path identified in Task 3.
- [ ] Export `Grid` in `src/ferrum/__init__.py` `__all__`.
- [ ] Tests: `Grid(...)` constructs per §3.19; gridline styling reaches SVG output.
- [ ] Verify: `uv run pytest -n auto -k grid` (+ cargo test if Rust touched)
- [ ] `/regression-test`

### Task 5: Item 19 — Raw scene type + producers (rust-coder)
- [ ] `types.rs`: `Raw { svg: String, anchor: RawAnchor }`, `enum RawAnchor { Chrome, Data }`, `#[serde(default)]` → Chrome (spec §6).
- [ ] Set `anchor`: `legend.rs` colorbar → Chrome; `inset.rs` → Chrome; `annotation.rs` image → Data when panel/data-space else Chrome.
- [ ] Confirm `svg_walk.rs` still emits `svg` verbatim, ignores `anchor` (static unchanged).
- [ ] Tests: producers set correct anchor; static SVG unchanged.
- [ ] Verify: `cargo test` (DYLD prefix) + static golden byte-equality.

### Task 6: Item 19 — WASM collect + JS overlay (rust-coder + JS)
- [ ] `scene_load.rs`: collect Raw (with anchor) into the scene-export channel that ships Text nodes; remove the `console.warn` skip.
- [ ] `ferrum-anywidget.js`: inject Raw fragments into the existing text overlay `<svg>`; per-fragment ID namespacing (`id="X"` → `id="ferrum-raw-{n}-X"` and matching `url(#X)`); Chrome fragments in a fixed `<g>`, Data fragments in a `<g>` receiving the canvas pan/zoom transform; overlay stays `pointer-events:none`.
- [ ] Rebuild WASM: `source ~/.cargo/env && wasm-pack build crates/ferrum-wasm --target web --out-dir ../../src/ferrum/_wasm/`.
- [ ] Tests (python-coder): continuous-color chart → `.interactive()` → exported bundle contains the colorbar gradient SVG (regression vs silent drop).
- [ ] Verify: `source ~/.cargo/env && cargo clippy -p ferrum-wasm --target wasm32-unknown-unknown -- -D warnings` + `uv run pytest -n auto -k interactive`
- [ ] `/regression-test`

### Task 7: Final gate + docs
- [ ] Full: `cargo test` (DYLD prefix), `uv run pytest -n auto`, `cargo clippy` (incl wasm target) all green.
- [ ] Visually inspect any changed goldens (`scripts/snapshot-goldens.py`, Read PNGs).
- [ ] Update archaeology doc: items 17/18/19/21 status columns + action-list entries 17/18/19; note item 20 still open.

## 6. Acceptance checks

- `uv run pytest -n auto` — all pass (hex stroke, Grid, label/text, interactive colorbar)
- `DYLD_LIBRARY_PATH=… cargo test` — all pass (Label kind, serde round-trip, Raw anchor)
- `cargo clippy -p ferrum-wasm --target wasm32-unknown-unknown -- -D warnings` — clean
- Static SVG goldens byte-identical for existing charts
- `mark_hex(stroke=, stroke_width=)` no longer raises; continuous-color `.interactive()` bundle contains colorbar gradient; scene JSON emits `"label"` for label marks
- Archaeology doc items 17/18/19/21 updated

## 7. Open questions

- Item 18: whether gridline rendering is already wired in Rust (Task 3 resolves before Task 4 begins); may shift Task 4 from python-only to python+rust.
