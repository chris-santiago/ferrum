# KG Issues #24–#27 Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use chris-code:subagent-driven-development to implement this plan task-by-task.

## 1. Objective

Close KG-6 (#24 facet col= default layout), KG-7 (#25 square-clip guard test + close as not-a-bug), KG-8 (#26 shape encoding honors sort), KG-9 (#27 EncodingSpec.condition getter).

## 2. Spec references

- `design-docs/superpowers/specs/2026-06-20-kg-issues-24-27-design.md` §6 (interfaces), §7 (invariants), §9 (acceptance)

## 3. Files

| Action | Path | Reason |
|--------|------|--------|
| Modify | `crates/ferrum-core/src/render/scale_resolve/auxiliary.rs` | KG-8: `build_shape_scale` applies sort + return `Vec<RenderWarning>` |
| Modify | `crates/ferrum-core/src/render/scale_resolve/mod.rs` | KG-8: `build_auxiliary_scales` shape arm `warnings.extend` |
| Modify | `crates/ferrum-core/src/render/scale_resolve/tests.rs` | KG-8: Rust unit (shape sort domain order) |
| Modify | `crates/ferrum-core/src/spec/encoding.rs` | KG-9: `condition` `#[getter]` (mirror `sort` getter) |
| Modify | `src/ferrum/_core.pyi` | KG-9: declare readable `condition` attr on `EncodingSpec` |
| Modify | `tests/test_core_stub_parity.py` | KG-9: remove write-only allowlist for `EncodingSpec.condition` |
| Create | `tests/test_shape_sort.py` | KG-8: Python render test (glyph↔category + legend order respond to sort) |
| Create | `tests/test_shape_clip_facet.py` | KG-7: guard test (faceted square glyph bbox ⊆ panel clip) |
| Modify | `src/ferrum/chart.py` | KG-6: `_Facet.wrap_orient`; `facet()` sets it; `_to_facet_dict` infers ncols for col-orientation |
| Modify | `tests/**` (faceted) | KG-6: update position-asserting tests + regenerate goldens for col-only facets |

## 4. Constraints

- **KG-6 — only col-only-without-ncols changes.** `facet(row=X)`, generic `facet(field=X)`, and any explicit-`ncols` facet are byte-identical to today. Preserve orientation on `_Facet` (a `wrap_orient: Optional[str]` frozen field surviving `_clone`/`__copy__`); `_to_facet_dict` wrap branch: `ncols = f.ncols if set, else _infer_facet_cardinality(f.field) when wrap_orient=="col", else 1`.
- **KG-6 goldens — visually inspected before commit.** Every regenerated `tests/**/*.svg` must be rasterized via `python scripts/snapshot-goldens.py <name>` and the PNG inspected to confirm side-by-side panels render correctly. The ORCHESTRATOR blesses PNGs; the coder reports SVG+PNG paths and must NOT self-bless. Use `regen_and_verify` from `tests/_snapshots.py` where a regen script exists.
- **KG-7 — no production code change.** Guard test only; use EXPLICIT `ncols` in the test so it is invariant to the KG-6 default change. Must fail if any faceted square glyph bbox exceeds its panel clip (real discriminator).
- **KG-8 — non-sort byte-identical.** Absent `sort` → first-appearance order unchanged. Mirror `color.rs` exactly: `SortContext { category_field: &shape_enc.field, batch: domain_batch, x_field/y_field from encoding }` + `apply_sort_to_domain(&mut distinct, shape_enc.sort.as_ref(), &sort_ctx, &mut warnings)`. Change `build_shape_scale` return to `Result<(Option<ShapeScale>, Vec<RenderWarning>), RenderError>`; the only caller is `build_auxiliary_scales` in mod.rs — update it to `warnings.extend(shape_warns)`. Faceted sort resolves against the `domain_batch` from `shared_categorical_batch` (already chosen).
- **KG-9 — mirror the `sort` getter** at `encoding.rs:528` exactly (serde→json→`json.loads`). Round-trip: `EncodingSpec(field, condition=c).condition == c`. No change to construction or the 5 concrete stub defaults.
- No matplotlib; no global mutable state; `cargo test` must pass.
- Build before pytest: `unset CONDA_PREFIX && uv run --no-sync maturin develop` (KG-8/KG-9 change Rust). Pytest gets **NO** `DYLD_LIBRARY_PATH`; cargo test -p ferrum-core needs `DYLD_LIBRARY_PATH=/opt/homebrew/Caskroom/miniforge/base/lib`. Source `~/.cargo/env` for cargo.

## 5. Tasks

### Stage 1 — parallel (disjoint footprints)

#### Task 1 (KG-8): shape encoding honors sort — Rust
- [ ] `build_shape_scale` (auxiliary.rs): build `SortContext` + `apply_sort_to_domain` per spec §6; change return to `Vec<RenderWarning>` (collect overflow + sort warnings).
- [ ] `build_auxiliary_scales` (mod.rs): shape arm `let (shape, shape_warns) = build_shape_scale(...)?; warnings.extend(shape_warns);`.
- [ ] Rust unit in `scale_resolve/tests.rs`: `shape` domain order follows `sort` (alphabetical "ascending"/"descending", data-aware "-y", explicit array); absent-sort = first-appearance (byte-identical). Fail-before/pass-after.
- [ ] `tests/test_shape_sort.py`: render test — `shape=fm.Encoding("g", sort=...)` changes glyph↔category assignment + legend order.
- [ ] Verify: `source ~/.cargo/env && DYLD_LIBRARY_PATH=/opt/homebrew/Caskroom/miniforge/base/lib cargo test -p ferrum-core --lib` ; `cargo clippy -p ferrum-core -- -D warnings` (no NEW lints) ; `unset CONDA_PREFIX && uv run --no-sync maturin develop && uv run --no-sync pytest tests/test_shape_sort.py -p no:randomly -q`

#### Task 2 (KG-9): EncodingSpec.condition getter — Rust + stub
- [ ] `encoding.rs`: add `condition` `#[getter]` mirroring the `sort` getter (spec §6).
- [ ] `_core.pyi`: add `condition` to `EncodingSpec`'s readable attribute block.
- [ ] `tests/test_core_stub_parity.py`: remove the write-only allowlist entry for `EncodingSpec.condition`; add/confirm a round-trip read assertion.
- [ ] Verify: `source ~/.cargo/env && DYLD_LIBRARY_PATH=/opt/homebrew/Caskroom/miniforge/base/lib cargo test -p ferrum-core --lib` ; `unset CONDA_PREFIX && uv run --no-sync maturin develop && uv run --no-sync pytest tests/test_core_stub_parity.py -p no:randomly -q`

#### Task 3 (KG-7): square-clip guard test — Python (test only)
- [ ] `tests/test_shape_clip_facet.py`: faceted scatter with a category mapping to `ShapeKind::Square` (palette idx 1) using EXPLICIT `ncols`; parse each panel's `<clipPath>` rect and every data `<rect>` glyph; assert every square bbox ⊆ its panel clip and its half-extent ≤ the circle extent for the same `size`. Real discriminator (would fail if squares exceeded the clip).
- [ ] Verify: `unset CONDA_PREFIX && uv run --no-sync pytest tests/test_shape_clip_facet.py -p no:randomly -q`

### Stage 2 — after Stage 1 (rebuild extension first)

#### Task 4 (KG-6): facet(col=) default layout — Python + golden fallout
- [ ] `chart.py`: add `_Facet.wrap_orient`; `facet()` sets `"col"`/`"row"`/`None`; `_to_facet_dict` wrap branch infers `ncols` for `wrap_orient=="col"` (spec §6). Thread `wrap_orient` through `_clone`/`__copy__`/reconstruction.
- [ ] Update position-asserting faceted tests to the correct orientation (do NOT pin the old vertical default); add a discriminating test: col-only panels horizontal (distinct cx ranges, shared cy), row-only vertical.
- [ ] Regenerate all col-facet goldens that byte-diff; run `python scripts/snapshot-goldens.py <name>` for each; report SVG+PNG paths for orchestrator blessing (do NOT self-bless).
- [ ] Verify: `unset CONDA_PREFIX && uv run --no-sync pytest tests/ -k facet -p no:randomly -q` then full suite.

## 6. Acceptance checks

- `cargo test -p ferrum-core` + `-p ferrum-wasm` green (orchestrator).
- Full pytest green after KG-6 golden regeneration + PNG inspection (orchestrator).
- KG-8: `grep -n "apply_sort_to_domain" auxiliary.rs` present; shape sort tests pass.
- KG-9: `EncodingSpec(f, condition={...}).condition` returns the value; parity test green without the allowlist entry.
- KG-6: `.facet(col="g")` (no ncols) → `mode.ncols == n_distinct(g)`; col panels horizontal; row panels vertical.
- KG-7: guard test passes; #25 ready to close with evidence.

## 7. Open questions

- KG-6: if a position-asserting test assumed the old vertical col-layout, update it to horizontal (the correct new default), not pin the old one. Golden blast radius (~107 col-facet sites; byte-diff subset regenerates) is handled in Task 4.
