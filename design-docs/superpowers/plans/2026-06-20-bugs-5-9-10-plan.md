# Bugs #5 / #9 / #10 Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use chris-code:subagent-driven-development for the Rust tasks (#5, #9). Task #10 is JavaScript (no language coder exists) — orchestrator implements + validates via headless capture.

## 1. Objective

#5 [FA-11]: mark_line honors fill_opacity + shared OpacityResolver (preserve semantics, Option A). #9 [FA-15]: build a color legend from conditional-only color encodings. #10 [R6]: fix interactive label z-order via DOM-order-preserving data-join.

## 2. Spec references

- `design-docs/superpowers/specs/2026-06-20-bugs-5-9-10-design.md` §6 (interfaces), §7 (invariants), §8 (decisions), §9 (acceptance)

## 3. Files

| Action | Path | Reason |
|--------|------|--------|
| Create | `crates/ferrum-core/src/render/marks/opacity.rs` (or sibling) | #5 `OpacityResolver` |
| Modify | `crates/ferrum-core/src/render/marks/line.rs` | #5 read+apply fill_opacity; use resolver |
| Modify | `crates/ferrum-core/src/render/marks/{area,point,bar,rect}.rs` | #5 use shared resolver (preserve semantics; bar keeps fallback) |
| Modify | `crates/ferrum-core/src/render/marks/mod.rs` | #5 register the new module |
| Modify | `crates/ferrum-core/src/render/prepare.rs` | #9 conditional-color legend helper + integration |
| Modify | `src/ferrum/_wasm/ferrum-anywidget.js` | #10 `_placeTextSvg` data-join + flag-clear |
| Test | `crates/ferrum-core/src/render/marks/{line,area}.rs` (mod tests) | #5 line+area opacity regression |
| Test | `crates/ferrum-core/.../prepare.rs` tests or a Rust integration test | #9 conditional-color legend regression |

## 4. Constraints

- **#5 Option A — preserve semantics.** The resolver's `at_row`/`at_group_first` outputs MUST equal each mark's current inline logic byte-for-byte (same defaults, finite-check, clamp to [0,1], and fallback). bar.rs ALONE keeps `fill_opacity ← opacity` fallback (`general_fallback=true`); area/point/rect/line do NOT add it. The ONLY intended behavior change is line.rs now reading+applying `fill_opacity`. Do NOT move scale transforms into the resolver — leave them at the call sites (point/rect).
- **#5 goldens** — only line+fill_opacity charts should byte-diff. If any golden changes, regenerate via `python scripts/snapshot-goldens.py <name>`, report SVG+PNG paths; ORCHESTRATOR blesses PNGs (coder must NOT self-bless). Confirm non-line opacity goldens are byte-identical.
- **#9 — gated on `provisional_scales.color == None`.** Base-color charts byte-identical. Walk `spec.conditionals` for `ChannelName::Color` branches; collect `EncodingValue::Field { name }` from `if_selected`/`if_not`; resolve via `distinct_values_in_order(transformed, name)`; dedup first-appearance order; emit `LegendEntry { label, symbol: SymbolKind::Circle }` + legend title = field name (match the categorical path). No WASM change. Regenerate+bless any conditional-color golden that gains a legend.
- **#10 — preserve text content/positions.** Only change the DOM update strategy (`selectAll().data().join()` instead of remove+append) and clear `el._ferrumLegendWired` in `_wireLegendToggles` before re-binding. All `_placeTextSvg` call sites inherit the fix. Do NOT change the GPU/canvas path.
- No matplotlib; no global mutable state; `cargo test` must pass; goldens visually inspected before commit.
- Build: `unset CONDA_PREFIX && uv run --no-sync maturin develop` (#5/#9 change Rust). Rust tests: `DYLD_LIBRARY_PATH=/opt/homebrew/Caskroom/miniforge/base/lib cargo test -p ferrum-core`. WASM: `source ~/.cargo/env && wasm-pack build crates/ferrum-wasm --target web --out-dir ../../src/ferrum/_wasm/` + `cargo clippy -p ferrum-wasm --target wasm32-unknown-unknown -- -D warnings`. Pytest: NO DYLD.

## 5. Tasks

### Stage 1 — parallel (disjoint footprints)

#### Task 1 (#5): mark_line fill_opacity + shared OpacityResolver — Rust
- Consumes: opacity-channel resolution shapes per spec §6 (load/at_row/at_group_first).
- [ ] Add `OpacityResolver` (new module) per spec §6; register in marks/mod.rs.
- [ ] Migrate point/bar/rect to `at_row`, line/area to `at_group_first`, byte-preserving each mark's current semantics (bar `general_fallback=true`; others false). line.rs additionally assigns resolved `fill_opacity` to its `FillStroke`.
- [ ] Rust units: line per-group fill_opacity differs (fail-before/pass-after); area per-group fill_opacity unchanged; spot-check point/bar/rect per-row unchanged.
- [ ] Verify: `source ~/.cargo/env && DYLD_LIBRARY_PATH=/opt/homebrew/Caskroom/miniforge/base/lib cargo test -p ferrum-core --lib`; `cargo clippy -p ferrum-core -- -D warnings` (no NEW); `unset CONDA_PREFIX && uv run --no-sync maturin develop && uv run --no-sync pytest tests/ -k "opacity or line or area" -p no:randomly -q`. Report any golden byte-diffs (SVG+PNG) for blessing.

#### Task 2 (#9): conditional-color legend — Rust
- [ ] `resolve_conditional_color_domain(spec, transformed)` in prepare.rs per spec §6; integrate in the `provisional_scales.color == None` legend arm (emit entries + title).
- [ ] Rust unit: spec with `color=None` + a `Color` conditional `Field` branch → non-empty `legend_entries` matching the field's distinct values; base-color chart unchanged.
- [ ] Verify: `source ~/.cargo/env && DYLD_LIBRARY_PATH=/opt/homebrew/Caskroom/miniforge/base/lib cargo test -p ferrum-core --lib`; `cargo clippy -p ferrum-core -- -D warnings` (no NEW); `unset CONDA_PREFIX && uv run --no-sync maturin develop && uv run --no-sync pytest tests/ -k "conditional or legend" -p no:randomly -q`. Report any golden byte-diffs for blessing.

#### Task 3 (#10): interactive label z-order — JavaScript (orchestrator-implemented)
- [ ] Rewrite `_placeTextSvg` as a D3 data-join (enter/update/exit, stable key, DOM order preserved); clear `el._ferrumLegendWired` in `_wireLegendToggles` before re-binding.
- [ ] Rebuild WASM: `source ~/.cargo/env && wasm-pack build crates/ferrum-wasm --target web --out-dir ../../src/ferrum/_wasm/`; `cargo clippy -p ferrum-wasm --target wasm32-unknown-unknown -- -D warnings` clean.
- [ ] Validate via headless capture (puppeteer-core + Chrome.app per project harness): a zoomed chart with overlapping label + annotation — confirm label paints above raw geometry after a zoom tick; confirm legend toggle still works.

## 6. Acceptance checks

- `cargo test -p ferrum-core` + `-p ferrum-wasm` green (orchestrator).
- Full pytest green after any golden regeneration + PNG blessing (orchestrator).
- #5: `grep OpacityResolver marks/*.rs` shows all 5 marks use it; line.rs applies fill_opacity.
- #9: `resolve_conditional_color_domain` present; conditional-color legend test green.
- #10: `_placeTextSvg` uses `.data().join()` (no remove+append loop); wasm builds; headless z-order check passes.

## 7. Open questions

- #5 composition-semantics deferred (spec §11) — not in scope.
- #10 is browser-only-validated (no automated test); acceptable for a cosmetic interactive fix.
