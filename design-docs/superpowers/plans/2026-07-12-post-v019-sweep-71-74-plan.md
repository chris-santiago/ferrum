# Post-v0.19 Sweep (GH #71–#74) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use chris-code:subagent-driven-development (recommended) or chris-code:executing-plans to implement this plan task-by-task.

## 1. Objective

Implement the four remediations pinned in the batch design spec: spelling-independent
independent-y semantics, unified per-slot y-domain resolution with layer-param
hoisting, slot-aware WASM hit-testing/relabeling (incl. GH #60), and nested-composite
shared resolve for color/size with consistent legend suppression.

## 2. Spec references

- `design-docs/superpowers/specs/2026-07-12-post-v019-sweep-71-74-design.md` (all §§; §6 contracts, §8 locked decisions, §9 acceptance)
- Evidence tests (committed RED at 7bd8ff50): `tests/test_bug_hunt_secondary_y.py`, `tests/test_bug_hunt_interactive_slots.py`, `tests/test_bug_hunt_shared_legend.py`, `tests/test_bug_hunt_figure_legend.py::test_explicit_tooltip_on_primary_layer_does_not_leak_to_secondary`, bug-hunt tests in `crates/ferrum-wasm/src/{hit_test,text_json}.rs`

## 3. Files

| Action | Path | Reason |
|--------|------|--------|
| Modify | `src/ferrum/composition.py` | capability-predicate consultation; conflict raising; docstring corrections (§8.10) |
| Modify | `src/ferrum/chart.py` | capability predicate; rename determinism + display name; SecondaryY mark guard |
| Modify | `src/ferrum/_spec_build.py` | tooltip-injector gate; `_collect_params` layer walk |
| Modify | `src/ferrum/configure.py` (+ Rust consumer if seam demands) | `orient="none"` → legend-disabled signal |
| Modify | `crates/ferrum-core/src/render/scale_resolve/` | relocated param substitution; shared per-layer y-resolution |
| Modify | `crates/ferrum-core/src/render/prepare/mod.rs` | slot plan computed+stored; axis inputs consume shared resolution |
| Modify | `crates/ferrum-core/src/render/scene_build.rs` | consume slot plan; drop private param pre-pass; emit slot tags on axis text |
| Modify | `crates/ferrum-scene/src/types.rs` (+ scene_load mirror) | optional slot field on axis-tick text nodes |
| Modify | `crates/ferrum-core/src/spec/composite.rs`, `render/composite.rs`, `render/composite_render.rs` | leaf-span union for color/size; effective-mode inheritance; band planner lockstep |
| Modify | `crates/ferrum-wasm/src/{spatial_index,hit_test,lib,selection_state,scene_load,text_json}.rs` | slot-aware hit-test; by-slot relabel; debug_assert |
| Test | evidence files above + new discriminating tests per spec §9 | flip red→green; pin new contracts |
| Modify | `ferrum-spec.md` §3.9, `design-docs/superpowers/specs/2026-07-12-composite-shared-legend-design.md`, `design-docs/superpowers/followups/2026-05-15-code-archaeology.md` | dated notes; SY-5/#60 close; back-links |

## 4. Constraints

- Param-free goldens byte-identical: `tests/goldens/secondary_y_axis/*.svg`, shared-legend goldens, `compute_layout_no_secondary_y_is_byte_stable_with_pre_52_shape`. Never re-bless a golden without rasterize+visual inspection per CLAUDE.md.
- Existing pins must stay green: x-shared no-raise (`test_secondary_y_axis.py:404`), multi-y-layer guard (`:425-452`), first-member third-axis stacking, default shared-y overlay path, pairplot/compare positional pairing, grid-hole handling, `.spec` introspection shapes (flat-dict pass-through unchanged).
- The two congruence-gated walks (`resolve_channel` in `render/composite.rs` and `plan_legend_walk` in `composite_render.rs`) must change in lockstep — bit-for-bit agreement contract documented at `spec/composite.rs:264-268`.
- x/y positional tree-path pairing semantics unchanged; leaf-span union applies to color/size only (spec §4D rule).
- Slot-list indexing conventions stay split per GH #63: `y_domains` 0-based incl. slot 0; `y_slot_levels`/`secondary_affines` 1-based. Do not unify.
- WASM hit-test with identity rescales must reproduce today's behavior exactly; slot tag is serde-default `None` (additive scene contract).
- No global mutable state; no warn-fallbacks; no NotImplementedError.
- Rust build/test: `unset CONDA_PREFIX && uv run --no-sync maturin develop`; `unset CONDA_PREFIX PYTHONPATH; DYLD_LIBRARY_PATH=$(uv run python -c "import sys; print(sys.base_prefix + '/lib')") cargo test -p <crate>` (sys.base_prefix, not sysconfig LIBDIR). Clippy judged by delta (~166 baseline).
- Do not touch files owned by the in-flight #69/#70/#75/#76 fixes: `crates/ferrum-core/src/scale/*`, `render/scale_resolve/positional.rs`, `render/position.rs`, `src/ferrum/plots/*` — coordinate through the orchestrator if a task genuinely needs one.

## 5. Tasks

### Task 1: Chart-level independent-y capability predicate + conflict raising (#71)
- [ ] Predicate on `Chart` per spec §6; consult in `_contains_independent_y_layer`, `_lower_any` leaf branch + shared-y check, `LayerChart` explicit-shared-y path (raise instead of force-shared)
- [ ] Flip red: HConcat/VConcat/LayerChart/deep-nesting conflict tests in `tests/test_bug_hunt_secondary_y.py`
- [ ] Verify: `uv run pytest tests/test_bug_hunt_secondary_y.py tests/test_secondary_y_axis.py tests/test_composition.py -q`

### Task 2: Rename determinism + display-name preservation (#71)
- Consumes: Task 1's chart.py state
- [ ] Deterministic suffix (counter/index) replacing `id()` for `__rhs_`; original column name as presentation title (and tooltip display per red-test assertions); no sentinel in SVG or scene JSON
- [ ] Flip red: `test_heterogeneous_data_secondary_axis_title_has_no_rhs_sentinel`, `test_scene_render_of_secondary_y_desugared_chart_is_one_panel`
- [ ] Verify: `uv run pytest tests/test_bug_hunt_secondary_y.py tests/test_golden_secondary_y_axis.py tests/test_secondary_y_axis.py -q`

### Task 3: SecondaryY primitive-mark guard (#71)
- Consumes: Task 2's chart.py state
- [ ] `ValueError` per spec §6 contract at desugar; new discriminating test (spec §9.1)
- [ ] Verify: `uv run pytest tests/test_bug_hunt_secondary_y.py tests/test_secondary_y_axis.py -q`

### Task 4: _spec_build fixes — tooltip gate + param hoisting (#71 item 3, #72 Python)
- [ ] Tooltip: chart-level explicit-tooltip short-circuit yields to per-layer loop when layers exist (spec §4A)
- [ ] `_collect_params` walks per-layer encodings, dedupe by name (spec §6 contract); new test: layer-declared param on wire exactly once when also chart-level
- [ ] Flip red: tooltip test in `test_bug_hunt_figure_legend.py`; `test_domain_param_on_secondary_layer_resolves_static_slot_domain`
- [ ] Verify: `uv run pytest tests/test_bug_hunt_figure_legend.py tests/test_bug_hunt_interactive_slots.py tests/test_layer_auto_tooltips.py tests/test_phase_11d -q`

### Task 5: Relocate param substitution into scale_resolve + shared per-layer y-resolution (#72 Rust)
- [ ] Move `resolve_param_domains` (scene_build.rs:1125) into scale_resolve beside `resolve_scales_with_leaf_context`; both `build_secondary_y_axis_inputs` and `resolve_layer_y_scale` consume one shared per-layer y-domain resolution (caller-supplied pixel range)
- [ ] New discriminating test: static right-axis ticks == substituted domain for param-bound secondary layer (spec §9.3)
- [ ] Verify: cargo test -p ferrum-core (full); `uv run pytest tests/test_bug_hunt_interactive_slots.py tests/test_golden_secondary_y_axis.py -q` after `maturin develop`

### Task 6: Layer→slot plan computed once at prepare (#72 Rust)
- Consumes: Task 5's shared resolution seam
- [ ] Plan stored on `PreparedInputs` (mirror `LegendBandPlan` rationale, composite_render.rs:456-472); consumed by prepare axis inputs, `resolve_panel_scales`, axis router (no position inference), `collect_param_bindings`
- [ ] Verify: cargo test -p ferrum-core; `uv run pytest tests/test_golden_secondary_y_axis.py tests/test_secondary_y_axis.py -q` (byte-identical goldens)

### Task 7: Slot tag on axis-tick text nodes (#73 / GH #60, scene contract)
- [ ] Optional slot field on axis-tick text nodes in ferrum-scene + scene_build axis-router emission + ferrum-wasm scene_load decode (serde-default None); slot-tag round-trip test (spec §9.5)
- [ ] Verify: cargo test -p ferrum-core -p ferrum-wasm

### Task 8: text_json selects rescale affine by explicit slot (#73)
- Consumes: Task 7's slot tag
- [ ] Retire frequency heuristic for slot identity (keep stray-label structural guarantee, spec §7); respect 1-based `secondary_affines`
- [ ] Flip red: `bug_hunt_single_tick_secondary_axis_relabels_under_slot_rescale`; controls stay green (`:480-499`, `:1084`, `:1131`, `:539`, `:571`)
- [ ] Verify: cargo test -p ferrum-wasm

### Task 9: Slot-aware hit-testing + debug_assert (#73)
- [ ] `y_slot` on `MarkEntry`; thread `slot_rescales`+`panel_slot_counts` from `hit_test_at` and selection click path; per-candidate composed inverse via `transform_slot_index`; mirror exemplar 22333c30
- [ ] Update red test to new signature; INVERT stale-position mirror test (its comment instructs); debug_assert before clamp in `transform_slot_index` (ferrum-core `mark_nodes.rs:149` idiom)
- [ ] Verify: cargo test -p ferrum-wasm (full — shared-contract change)

### Task 10: Leaf-span union for shared color/size + inheritance rule (#74 Rust)
- [ ] `resolve_channel`: union across entire leaf span at effective-shared color/size nodes (reuse `flatten_leaves`); explicit-wins/unset-inherits effective-mode rule (spec §6); `plan_legend_walk`/`descend_channel` change in lockstep (band at outermost effective-shared node)
- [ ] Flip red: 3 nesting-shape tests in `tests/test_bug_hunt_shared_legend.py`; new test: explicit-independent child under outer shared stays independent
- [ ] Verify: cargo test -p ferrum-core; `uv run pytest tests/test_bug_hunt_shared_legend.py tests/test_composite_shared_legend.py tests/test_composite_shared_legend_goldens.py tests/test_phase_9_e2e -q` after `maturin develop`

### Task 11: orient="none" joins legend-disabled path (#74)
- [ ] Map `configure_legend(orient="none")` onto the `Color(legend=None)` disabled mechanism (seam per spec §8.9 — no new LegendOrient variant); composite band + panel legends suppressed; single-chart legends suppressed; new single-chart test
- [ ] Flip red: `test_composite_level_configure_legend_orient_none_suppresses_band`
- [ ] Verify: `uv run pytest tests/test_bug_hunt_shared_legend.py tests/test_override.py tests/test_composite_shared_legend.py -q`; cargo test -p ferrum-core if Rust touched

### Task 12: Docs + docstring close-out
- [ ] `.spec` docstring corrections (composition.py:238-249, :494-503 — state flat-dict raw-view exception); `ferrum-spec.md` §3.9 dated note (nested resolve rule, orient="none"); #16 design-doc nesting section update; archaeology doc: SY-5/#60 closed, `__dodge_n_groups__` back-link, #52 §4 spelling-independent note
- [ ] Verify: `uv run pytest tests/test_docstring_coverage.py -q`

## 6. Acceptance checks

- `uv run pytest -n auto` — full suite green (all evidence tests flipped, zero `# BUG` markers remaining for #71–#74)
- `DYLD_LIBRARY_PATH=... cargo test -p ferrum-core -p ferrum-wasm` — green (both red Rust tests flipped; mirror test inverted)
- Byte-identity: `git diff --stat tests/goldens/` empty (or any diff rasterized + visually inspected)
- Spec §9 criteria 1–8 all satisfied

## 7. Open questions

- Tooltip display-name handling for renamed columns (title-only vs. field-label map) — resolve in Task 2 against the red test's assertions.
